import { Text, View } from 'react-native';
import GlobalStyle from '../../../styles/Style';

const ReportForm = ({ navigation }) => {
   return (
      <View style={GlobalStyle.Container}>
         <Text>Report Form Screen</Text>
      </View>
   );
};

export default ReportForm;
